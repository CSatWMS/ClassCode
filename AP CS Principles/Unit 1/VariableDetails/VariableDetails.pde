//this is a comment

/*
  this is called a block comment
*/

//creating variables
  //dataType variableName = value;
int numberOfStudents = 14;

/*
primitive data types
int -> whole numbers (biggest = 2 billion ish)
float -> decimals (stores up to 6 decimals)
long -> bigger whole numbers 
double -> bigger decimals (store up to 16 decimals)
char -> single characters (get single quotes)
byte -> 8 bits 
boolean -> only true / false
short -> smaller whole numbers (16 bits)

reference data type (fancier)
String -> text (more than one letter) get double quotes
*/

float gpa = 97.7;
println(gpa);
println(Integer.MAX_VALUE);
println(Long.MAX_VALUE);
println(Double.MAX_VALUE);
println(Short.MAX_VALUE);

println(Integer.MAX_VALUE + 1);
//integer overflow
println(Integer.MIN_VALUE);

char desiredLetterGrade = 'A';
String myName = "Ms. Snyder";
println(desiredLetterGrade);
println(myName);
println(Float.MAX_VALUE);
println(Integer.MAX_VALUE);

/*
Variable Naming Rules
  Rule = something you HAVE to do
  1. No Spaces
  2. alphaNumeric (letters and numbers, also use the Under_score)
  3. cannot be reserved words (the orange ones)
  4. must start with a letter (not a number)

Variable Naming Conventions
  Convention = a suggestion, something you should do
  1. Descriptive
  2. Start with a lowerCase letter
  3. Deal with the no space thing
      a. toUseCamelCaseToDisplayWords
      b. using_under_scores_for_spaces
*/
